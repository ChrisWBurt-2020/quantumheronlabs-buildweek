{
  "name": "heron-buildweek-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "node --check script.js && node --check heron-mascot.js && node --check heron-particles.js && node scripts/validate-site.mjs",
    "test": "node scripts/validate-site.mjs"
  }
}
